# 入力不可 トラブルシューティング

## 警告が表示される

原因:

- 設定済みフィールドが削除された
- 設定済みフィールドのフィールドコードが変更された
- サブテーブルまたはサブテーブル内フィールドが削除された

対応:

- 警告の設定番号とフィールドコードを確認する
- プラグイン設定で対象フィールドを再設定する

## 入力不可にならない

原因:

- プラグイン設定が保存されていない
- 対象フィールドが現在のフォームに存在しない
- フィールドタイプごとのDOM構造が想定と異なる

対応:

- 設定画面で対象フィールドを選び直す
- 背景色設定を保存し直す
