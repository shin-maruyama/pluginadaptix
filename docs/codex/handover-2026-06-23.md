# 2026-06-23 Handover

## 実施内容

- Issue #18 対応として、WordPress 連携のプラグイン一覧ショートコードにダウンロードボタンを追加。
- `downloadAvailable=true` のプラグインのみボタンを表示し、押下時に `POST /v1/plugins/download-token` を呼び出して `downloadUrl` へリダイレクトする処理を追加。
- 契約期限切れ、ライセンス期限切れ、APIエラー、ライセンスキー未解決時のメッセージ表示を追加。
- README にダウンロード機能、ライセンスキー解決方法、動作確認手順を追記。

## 変更ファイル

- `apps/wordpress-integration/src/PluginLicenseListShortcode.php`
- `apps/wordpress-integration/README.md`
- `docs/codex/handover-2026-06-23.md`

## 検証

- `pnpm lint`
- `pnpm typecheck`
- `pnpm test`
- `pnpm build`

## 未完了事項

- PHP 実行環境が見つからず、`php -l` による構文チェックは未実施。

## 次回作業

- `docs/codex/next-tasks.md` を参照。

## 注意事項

- `.env` は作成していない。
- 秘密情報、APIキー、実ライセンスキーは追加していない。
