# TAB表示プラグイン秘密鍵 対応手順書

## 1. 目的

Git管理下で確認されたTAB表示プラグインの秘密鍵について、内容を開示せず影響範囲を特定し、安全な順序で利用停止、保管移行、Git履歴除去を行うための手順を定める。

秘密鍵は現在のGit indexとローカルファイルから削除し、ローカルおよびGitHubリモートの全ブランチ履歴からも除去済みである。

## 2. 確認結果

| 項目 | 結果 |
|---|---|
| 隔離先 | `_repository_split_pending/plugins/TAB表示/TabDisplay.ppk` |
| ファイル形式 | PEM RSA private key |
| 鍵長 | 1024-bit |
| 整合性検査 | `RSA key ok` |
| ローカル権限 | `0600` |
| 対応する公開鍵 | `pluginadaptix_plugins/plugins/TAB表示/TabDisplaye/PUBKEY` |
| 通常版公開鍵 | `TabDisplay/PUBKEY` とは不一致 |
| 初回追加 | `c4c196351fc3ef7a2869377ee4690d4800c35980`、2026-06-10 |
| 日本語フォルダへの移動 | `453d80e0b0f7f50ba0f8c6b34b1a16030ac2457b`、2026-06-27 |
| 分割時の隔離 | `175c10dcc549839c72c92f6e5e3f4204c0655119`、2026-07-16 |
| リモート到達 | 初回追加コミットは `origin/master`、`origin/develop` など複数のリモート追跡ブランチに含まれる |
| 配布状況 | 2026-07-16確認: `TabDisplaye` は顧客および本番環境へ未配布 |
| 現在のGit index | 2026-07-16に追跡対象から除去済み |
| ローカル隔離ファイル | 2026-07-16に削除済み |
| Git履歴 | 2026-07-16にローカルとGitHubリモートの全5ブランチから除去済み |

公開鍵のSHA-256指紋による照合だけを行い、秘密鍵本文、秘密指数、素数、modulusは出力・記録していない。

## 3. リスク評価

- 秘密鍵はリモート履歴から取得可能だったものとして扱う必要がある。
- 対応公開鍵が難読化版プラグインに含まれるが、顧客および本番環境への配布実績はない。
- 鍵を削除するだけでは、既に取得された複製を無効化できない。
- 配布済み利用者の更新移行は不要だが、旧鍵は漏えい済みとして廃止し、今後の署名へ使用してはならない。
- 1024-bit RSA鍵を今後の新規配布へ継続利用しない方針を検討する必要がある。

## 4. 推奨実施順序

### Phase 1: 所有者・利用状況確認（完了）

1. 2026-07-16、`TabDisplaye` は顧客および本番環境へ配布していないことを確認した。
2. 配布済み利用者が存在しないため、既存利用者向けのプラグインID移行は不要と判断した。
3. 同じ鍵を別用途で利用していないことの組織内確認は引き続き必要である。

秘密鍵ファイル自体をメール、チャット、Issue、PRへ添付しない。

### Phase 2: 廃止・移行方針決定

1. 旧鍵を廃止し、今後の署名へ使用しない。
2. 新しい署名鍵はGit外の秘密情報保管先で生成・保管する。
3. 将来 `TabDisplaye` を配布する場合は、新しい鍵で生成した配布物の公開鍵、プラグインID、更新経路を新規配布前に検証する。
4. 旧鍵で新しい成果物を署名しない運用へ切り替える。

### Phase 3: 現在のGit管理対象から除去（完了）

2026-07-16、秘密鍵をGit追跡対象から除外し、ローカル隔離ファイルを削除した。

```bash
git rm --cached _repository_split_pending/plugins/TAB表示/TabDisplay.ppk
```

`.gitignore` には `*.ppk`、`*.pem`、`*.key` を追加済みである。

### Phase 4: Git履歴除去（完了）

専用ミラーで対象の旧パスと隔離後パスを除去し、検証後にGitHubリモートの全5ブランチをforce pushした。続いてローカル作業コピーにも同じ履歴書き換えを適用した。

```bash
git filter-repo \
  --path plugins/TabDisplay/TabDisplay.ppk \
  --path plugins/TAB表示/TabDisplay.ppk \
  --path _repository_split_pending/plugins/TAB表示/TabDisplay.ppk \
  --invert-paths
```

対象ブランチは `master`、`develop`、`docs/release-v1.0.0-rc1`、`feature/1-monorepo`、`feature/19-api-integration-tests` で、タグは存在しなかった。GitHub管理の `refs/pull/2/head` は秘密鍵追加前の `feature/1-monorepo` と同じコミットであり、対象秘密鍵を含まないことを確認した。既存クローンには古いオブジェクトが残る可能性があるため、利用者は再クローンする。

### Phase 5: 完了確認（完了）

- `git rev-list --all --objects` に対象3パスが存在しないことを確認した。
- 旧秘密鍵オブジェクトがローカルと履歴除去用ミラーに存在しないことを確認した。
- 全コミットの高確度シークレットスキャンで秘密鍵が検出されないことを確認した。
- 新しい3管理単位に秘密鍵ファイルが存在しないことを確認した。
- 旧鍵を使った新規署名を停止した。
- 顧客および本番環境へ未配布であることを記録した。

## 5. 実施結果

ローカル鍵削除、`git filter-repo` による履歴書き換え、全リモートブランチのforce pushを2026-07-16に実施した。顧客・本番環境への配布実績がないため、配布先との移行作業は不要である。将来 `TabDisplaye` を配布する場合は、Git外で新しい署名鍵を生成し、新規プラグインとして検証する。
